##### impl:p1-identity
branch: track/p1-identity
summary: P1.1, P1.2, P1.6 and P1.8 landed on track/p1-identity (branched from ecba321c; that ancestry is checked). P1.7 did not land, so the rotl listing still makes its false claim. The worktree is detached at 8c514ac1 and nothing was pushed.

P1.1, bit identity (a7da5198, 4e9744e1, b07e7c3a)
- The new `crates/r2ssa/src/view.rs` computes one fact per value: view(v) = (root, prefix_bits, ext in {Exact, Zero, Sign, Unknown}). It is solved over strongly connected components in O(V+E) with no iteration cap. It replaces the identity rules of `canonical_value_roots` and `adapt_root_width`, and deletes `canonical_root_in`, `insert_canonical_root` and `SSAVar::with_size`.
- These all read the view now: `same_bits`, the copy class (in predicates.rs, and range narrowing in values.rs, which is keyed by copy class), `constant::root_of`'s step rule, and `same_integer_root`. The `or_else(root_of)` workaround in shared.rs is deleted.
- The stack-reload certificate carries a relation, Identity or Derived. Only Identity enters `reload_values`, r2types' Parameter binding, signature home aliases and stack return carriers.
- In r2types prepared.rs, the two depth-8 recursions are replaced by `AddressBases`, one memo per function that reads the view. The test that treated a loop phi as the parameter is now a negative test.

P1.8, callee reach (8417ecdd)
- New `interproc/dependence.rs`: a bitset of which formals each value depends on.
- An access at no stated place, a value handed to a call, or a store int
tests: Final tree 8c514ac1, run as `cargo test -p r2ssa -p r2types -p r2engine -p r2dec --all-features --no-fail-fast`: 1572 passed, 0 failed, 2 ignored (summed over every "test result" line).

- `cargo fmt --all -- --check`: clean.
- `cargo clippy -p r2ssa -p r2types -p r2engine -p r2dec --all-features --all-targets -- -D warnings`: clean.
- `cargo build -p r2s --features sleigh`: OK. The binary embeds only this worktree's path.

Structure lints: I did not run scripts/structure-report.sh as written. I
not_done: ["P1.7 (Unspecified(width) leaf) is NOT landed, so the required rv_O2 rotl check still fails: `r2s rv_O2 -c 's sym.rotl; pdf'` still prints `mov ecx, esi ; defines rcx in [0x0, 0xff] (solved)`. The attempt minted INSERT(Unspecified, formal, off) and rendered reads of unspecified bits as residuals. It ", 'objects.rs (every Call/CallInd clobbers escaped-unknown) has no test that fails on the parent: every call with a graph target gets call-site facts, and I found no fixture without them.', 'No perf regression test (a synthetic slot with 2000 stored values through the binding plan). The sweep is checked for correctness against pairwise interference only.', "The lane joins through the lifter's projection are exercised only by hand-built SSA fixtures; renaming already turns register lanes into SUBPIECE/INSERT, so on lifted code they barely act.", 'Only the two r2engine native tests were actually run on the parent; the other failing-on-parent claims are argued, not run (see tests).', 'scripts/structure-report.sh was not run in its own whole-workspace target directory, for lack of disk (see tests for the substitute measurement).', 'Build hazards: the shared target directory fingerprints workspace crates by relative path, so I touched every source before each build and checked the worktree path embedded in the binary. Earlier in the session I evicted /home/user/wt-target/shared/debug/incremental (under the cargo lock) three tim']
##### impl:p1-reads
branch: track/p1-reads
summary: All three items landed on track/p1-reads, but the equivalence gate criterion is NOT met: 10 functions went from equal to refused and 8 from equal to residual-trap. Causes are below. Branch is from ecba321c, four commits, never pushed, worktree left detached at 44ff8cf5.

1. SystemReserved (1387aac3). There is a new r2abi platform table with psABI citations: x86-64 SysV fs_base plus the x87 control word and MXCSR control bits, i386 gs_base, AArch64 Linux tpidr_el0, and Darwin arm64 x18. SourceCallEffect now has three states (Clobbered/Preserved/SystemReserved), and a call never mints a CallDefine for a reserved register. Sleigh's FS_OFFSET/GS_OFFSET/FPUControlWord are mapped in r2sleigh-lift. The cc sdb is untouched. Both canary loads in rv main now read FS_OFFSET_0; before, the second one read FS_OFFSET_15, which nothing assigns.

2. Definite assignment (795150e8 + 8ad75ede).
- BindingPlan::unspecified_read, keyed on the SSA version, marks what a read cannot name: version 0 of a non-parameter register, and a CallDefine that no result claims.
- Every such read becomes r2sleigh_residual_T(site) with its cause, and the seal re-derives the map.
- Deleted: CallClobberedDeclaration, the binding-level call_clobbered flag and its seal check, the EntryValue exemption in binding_is_entry_declared, and the flow-insensitive "written somewhere" excuse.
- Preserved-register saves are fixed where they originate, in r2ssa: saved_register_slots recognises a slot a preserved register is saved 
tests: Final code, --no-fail-fast, touched crates: r2dec 345 passed / 0 failed / 1 ignored; r2engine 254 / 0; r2s + r2ssa 742 / 0 / 1 ignored; r2abi + r2source + r2sleigh-lift 168 / 0 / 8 ignored.

Tests added:
- seal.rs reaching_tests (8): an injected stale read is found and split; a loop variable read after its update is stale; a phi value is held only where every incoming edge delivers its input; an overwritten entry value evicts what overwrote it; an unsplittable read; a copy holds its source; copi
not_done: ["Equivalence gate criterion not met: 10 equal->refused and 8 equal->residual-trap. The residual-trap group needs the PE track's result widths. The void-return group needs return-kind inference (r2ssa interface recovery). The unreachable-path group (crc32_init, manual_limits clang-O1) needs checks tha", "No AST markers for elided definitions. Coalesced copies, restores and reloads are placed among the variable's rendered writes by instruction order. That is exact for this check, but no marker was added to the AST.", "A reload of a variable's own frame object is taken to hold whatever the variable holds; that is not re-checked against memory SSA. Stack-access reads (memory, not SSA values) are not checked by reaching values.", "A split re-renders the whole function. Only the split variable's group can change, but the rebuild is not limited to it.", 'The d+2 bound is argued, not measured: the loop runs to a fixed point with no pass budget.', 'A phi fed by an unspecified entry value across a branch still refuses rather than getting a path-specific residual; that needs the edge copy placed on its own edge.', "Version-0 reads of preserved registers outside save/restore are residuals too (stricter than the spec's 'non-preserved').", 'tests/coverage/run_coverage.sh not run: it uses macOS `clang -arch` and a release build into the worktree target.', 'structure-report baseline not re-blessed; the counts went down, but I did not record that.']
##### review:p1-identity
verdict: REQUEST CHANGES. The core of P1.1, P1.2 and P1.6 holds up: the view's transfer rules, the optimistic SCC solve, the joins in ValueContent, the relation on reload certificates and AddressBases are all sound as far as I could check, and the mul_div fix is real (a 13-vector harness gives 0 mismatches at tip and 5 at base). But the track cannot merge yet, for four reasons. (1) The new callee-reach rule is unsound. I proved it with a failing native test, and it is a regression from base. (2) The full-corpus equivalence ratchet fails: one function left `equal` (bisected to ac876632), and the implementer only ran review.c. (3) P1.7 is missing, so the false rotl claim remains. (4) crates/r2ssa/src/a
 - BLOCK: P1.8 is unsound when a formal is laundered through a frame object. In the Store/StoreConditional/AtomicCAS arms (~2380-2473), `memory_access_is_local_stack` hits `continue` before `bits_of_var(val)` is added to `unplaced_reach`. Separately, `FormalDependence::passed_to_call` (interproc/dependence.rs) only counts the bits of the values actually handed to the call. So storing a formal into a local and passing the local's address hides the formal. Counterexample: `void f(char *buf, void (*cb)(char* | FIX: A formal-derived value stored into a frame object that is not proven private must put its formals into `unplaced_reach`. Proven private means non-escaping per the object model or promotion's `escaped` set: the address is not handed to a call, stored, or returned. Compute `bits_of_var(val)` before th
 - BLOCK: The equivalence ratchet fails on the full census. Command: `tests/equiv/run_equiv.py --r2s <tip> --baseline tests/equiv/baseline.json`, with the baseline from 3fbea1dc, 756 records, 855 s. Result: 571 equal against the baseline's 565, but `tests/corpus/values.c::clang-O0::value_abs_minmax: left equal (now refused)`. The refusal is `observation journal: ConflictingValue`: ValueId(265), the merge of promoted slot stack:m112 ([rbp-0x68], the min() ternary temp), is recorded as bound to binding 5 an | FIX: Find why the cross-block content pairs make the binding plan inconsistent for value_abs_minmax, and fix that in r2ssa's pairing or in the r2dec binding plan until the function grades `equal` again. Then rebase onto 1e53fc79, re-run the full ratchet, and re-bless the three `main` records with their P
 - BLOCK: P1.7 (the Unspecified(width) leaf, task item 4) is not landed. Its required must-fail check still fails: `r2s rv_O2 -c 's sym.rotl; pdf'` prints `mov ecx, esi ; defines rcx in [0x0, 0xff] (solved)`, a false fact presented as solved. mint_entry_lane_projections (~243, ~405-470) still makes up the upper bits of partial entry-lane writes. | FIX: Land INSERT(Unspecified, formal, off) in mint_entry_lane_projections, make values.rs treat Unspecified as top, and have r2dec render reads of Unspecified bits as a counted residual. If, as the implementer found, that needs PE's interface widths first, amend the plan to add the hard edge 'P1.7 after 
 - BLOCK: Address provenance is not yet a projection of the view (task item 1 and the plan's P1.1), so a second owner of the identity rule remains. `derive_op_expression` (~486-501) carries an AddressExpression through IntSExt and through Cast/New of any width. `compute_scalar` (~606-613) treats IntSExt, IntZExt and a truncating SUBPIECE(x,0) as the same affine scalar. interproc `classify_address_root` asks `addresses().parameter_expression` before its own view-based steps, so the summary's 'address class | FIX: Read ValueViews in address.rs. An AddressExpression follows only same-width Copy/Cast and zero-extension chains to a full-width root (`same_integer_root`). An affine scalar treats sign extension and truncation as opaque terms, or uses an explicitly sign-aware domain. Add a unit test in which SEXT(la
##### review:p1-reads
verdict: REQUEST CHANGES. Do not merge track/p1-reads (44ff8cf5) yet.

The SystemReserved plumbing is sound for application code, the definite-assignment rewrite is sound, and the track's key suites pass: r2dec lib 342/0, r2engine native.rs 47/0, r2s pddj 3/0 and review 4/0, fmt clean.

**The new reaching-values validator is not sound.** It certifies silent miscompiles on trivial variants of mul_div. When it splits a variable, it can even create a copy out of a variable that has already been overwritten. I confirmed this by compiling and running the rendered C.

**The task's equivalence criterion fails.** I ran the full gate with the ratchet (tests/equiv/baseline.json from engine/review-fixes-vfmgfv)
 - BLOCK: Silent miscompile that the validator certifies: reloads of a frame object and stack-access reads are never checked.  Repro: - gcc or clang `-O0 -g` (DWARF parameter homes), scratchpad p1r/md/mdvar.c. - md1 `a*7/b + a%b + a` renders `RAX_1 = RAX_5 + RDX_5 + (uint64_t)a;` after `a = (int64_t)(tmp_3d800_2 >> 64)` (the cqo sign word). md1(7,3)=17 where the source gives 24, and md1(100,-9)=-76 where it gives 24. - md3 `a%b + a/b + a`: the split itself creates `uint64_t tmp_11f80_4 = (uint64_t)a;` aft | FIX: Model whether each frame-object binding is in sync with its slot: - A rendered write whose value is not stored into the object puts the variable out of sync with the slot. - A store (or an elided store of the value the variable holds) brings it back in sync. - A Content reload, or a StackAccess read
 - BLOCK: The validator runs on the machine CFG but places folded reads at the using instruction's block, not where the text evaluates them.  `FinalBindingRead.block` for a `Use` is the instruction's block. Placement's own `use-region-mismatch` evidence shows these reads are rendered in a region with a different entry: - rv_O0g `fact`: the imul reads of `n` are rendered in the return at 0x138f. - 7 of about 120 fixture functions have such reads (code_pointer_table_O0 0x100000420, and manual_limits_O0/O2 0 | FIX: Key each RenderedRead and RenderedWrite on the block where the text renders it: - Carry the rendered block in FinalObservationScope, or use `regions.node(region).entry()` for a Block region. - Set ordinal None when that block differs from the instruction's block. - Keep elided definitions at their i
 - BLOCK: An elided extension is treated as held wherever its narrow source is held. That is not syntactic identity, which the task requires.  `elided_definitions` maps IntZExt and IntSExt to `ElidedSource::Value(src)`, and `rename` then inserts sext(u) wherever u is must-held. A variable written with w = zext(u) has u in the closure of w, so the variable is taken to hold sext(u) while C holds zext(u). The unit test `review_sext_elided_over_a_zext_write_is_not_flagged` (in the scratchpad seal_with_review_ | FIX: - Rename only through Copy and CallRestore. - An elided ZExt or SExt is held only where the variable must-holds a value with the same opcode, source and width. Otherwise leave it out, so a read of it is stale and gets split. - Keep the closure direction (ext(u) holds u) as it is.
 - BLOCK: The equivalence ratchet fails, which the task ("moves no function away from equal") and plan decision 3 ("the function is not refused") both forbid.  Full gate on 44ff8cf5 against engine/review-fixes-vfmgfv's baseline: exit 1 with 18 records leaving equal.  10 moved equal→refused: - inverted_void: clang-O0, clang-O1, clang-O2, gcc-O0 - and_chain_effect: clang-O0, gcc-O0 - crc32_init: clang-O0, gcc-O0 - manual_limits clang-O1: state_machine, table_walk  8 moved equal→residual-trap: - sext: gcc-O1 | FIX: - Place the residual copy for a merge input that names an unspecified version on that incoming edge, splitting the edge in the structured output rather than putting the copy before the branch. That should bring crc32_init back to equal and make inverted_void and and_chain_effect residual-trap instea
 - BLOCK: Task item 2 is only partly met: a noreturn function that saves a callee-saved register still traps on its first statement.  `saved_register_slots` requires some load that restores the register, and a noreturn function has none.  Repro, scratchpad p1r/nr/nr.c, gcc -O1 and -O2: `die()` does push rbx, snprintf into a local buffer, then exit. It renders `stack_m8 = r2sleigh_residual_u64(1);` as its first statement. This behaviour also exists on the parent, so it is not a regression, but the task ask | FIX: Accept a save slot when every path that returns restores the register from it. That is vacuously true for a body with no returning path. Or treat a store of a preserved register's entry value (by copies) into an exact frame place that no program load reads as compiler-inserted, and count it.  Add a 
##### impl:p3-decl
branch: track/p3-decl
summary: Branch track/p3-decl has 8 commits on top of engine/review-fixes-vfmgfv (ecba321c). The worktree is detached at dd52b3da and nothing was pushed.

What landed, by part of the spec:
- **3.3, r2source (0f9d0e1f):** the source type graph moved to `crates/r2source/src/type_graph.rs` (schema version 2).
  - It now checks a layout the compiler stated only for consistency, so packed structs, i386 doubles aligned to 4, 80/96-bit floats, bit-fields, flexible arrays (`count: None`), `Code{signature_id}` and `Opaque{tag_id}` are all accepted.
  - `closure(roots)` renumbers the graph and drops anything unreachable instead of refusing it.
  - The interface constructor closes the graph, and an exact interface must state a logical value for every parameter and for a register result.
  - Matching arms were added in r2ssa (machine_context serialisation, the new kinds re-exported), r2types and r2engine.
- **Return owner, r2ssa (40fa2212):** if an interface types its values but gives its register result no type, no return value is certified. It no longer falls back to the carrier's width.
- **3.1, 3.2 and 3.4 (6fac2f1c):**
  - **One declaration model:** `r2abi::TypeGraph`, with scalars sized by width class, pointers, arrays with optional counts, bit-accurate records, enums as their underlying integer, `Code{signature, variadic, prototyped}`, `Opaque{keyword, tag}`, qualifiers including volatile, typedefs, and a refusal per node.
  - **sdb spellings:** read once into the same graph (`spelling.rs`
tests: All runs used the track's own target directory (/home/user/wt-target/p3-decl).

Final run on the touched crates: `cargo test -p r2abi -p r2image -p r2source -p r2ssa -p r2types -p r2dec -p r2engine -p r2s --all-features --no-fail-fast` passed with rc=0.
- r2abi: 38 passed
- r2image: 39 passed
- r2source: 43 passed
- r2ssa: 645 passed (includes the new return-certificate test)
- r2types: 324 passed
- r2dec: 336 passed, plus 1 doc test passed and 1 ignored
- r2engine: 254 passed
- r2s: 103 passed

not_done: ["No explicit refusal of a declared local at the return-address or stack-canary slot (spec 3.4). The declared frame drops only overlapping declarations. main's canary is still read as residuals (FS_OFFSET_0/FS_OFFSET_15). This belongs with the P4 frame partition / SSP elision work.", 'A function address passed as an argument is not named: main renders `dispatch((int32_t(*)(int32_t, int32_t))0x11a9, (int32_t)argc)` rather than `dispatch(add, ...)`. That needs a code-address naming fact (call contracts, P7).', "Also on function pointers: r2types uses a bare `CTypeLike::Function` to mean a pointer to code, while parts of r2dec's convert.rs treat `Pointer(Function)` as the function pointer. dd52b3da fixes only the constant case, so a non-constant integer passed to a function-pointer parameter is still not ca", "Found by manual check and present in the baseline too: a load through a `const double*` renders `r2sleigh_float_from_bits_64(values[i])` (two_units_O0g at 0x117c, and very likely rv avg). This converts a double's value to uint64 and then reinterprets the bits, which gives a wrong result. It is the l", 'two_units helper at 0x1149 renders the one declared local `doubled` as `int32_t doubled_2` plus a second `uint32_t doubled` binding (naming/binding work, P1/P4).', '`afv` shows the declared types but `stack_mN` names (e.g. `var uint32_t[4] stack_m200`), while `pdd` names the same slots `v`, `c`, `buf`: the two views disagree on names.', "Intended behaviour change: sdb library prototypes no longer apply to a program's own body by name. A stripped `main` no longer gets the sdb `main(int, char**, char**)` arity; `crates/r2s/tests/signatures.rs` was relaxed for that, and an r2engine test asserts that a body named `strlen` takes nothing ", 'Placement handles only 32/64-bit floats, integers up to pointer width, and pointers. A declaration with an x87 long double, __int128, a struct passed by value or a va_list parameter gets no declared interface; this is a visible refusal, not a guess.', 'Typedef names (size_t and the like) are not carried as SourceTypeGraph aliases; the spelling travels beside the signature.', "A data object's type still crosses `SourceDataObject` as a C spelling that r2types parses again. A typed object type on that contract was not added.", 'printf is still declared non-variadic, with its arguments dropped (`printf((const int8_t*)"%d %s\\n")`). This is unchanged from the baseline and is call-contract work.', 'Not run: the coverage sweep (`tests/coverage/run_coverage.sh`), `scripts/quality-gate.sh` (Kani, dylint, mutants), the whole-workspace suite and anything in `../radare2`. The structure baseline was not re-blessed, even though counts fell, to avoid conflicts with parallel tracks.', 'Commit 6fac2f1c is large: it carries the declaration model, the DWARF reader, address keying, per-item interning, the declared frame, frame-pointer distance and the restated merge together, because the engine rewrite was one piece. Splitting it would have meant intermediate code that never existed.', 'The gcc-O2 classify result flips between ub and differs across equivalence-gate runs with byte-identical renderings (UBSan misaligned address / vector choice). This is gate variance and was not investigated.']
##### impl:p2-container
branch: track/p2-container
summary: Track p2-container is finished on branch track/p2-container: 14 commits on ecba321c (confirmed as an ancestor). The worktree is detached at b72cb108 and clean. Nothing was pushed.

What landed, by spec item:
- **2.1** Container statement types now live only in `r2abi::statement`. r2image produces them; the engine's `Source` and the r2s session return `image.container()`. The duplicate definitions are deleted, and an architecture test guards against new ones.
- **2.2** There is one reader for relocation records. Each record is identified by where its bytes are in the file, which keeps `.rela.iplt`. The reader covers REL, RELA, RELR, Android APS2, JMPREL, Mach-O rebase/bind opcode streams and chained fixups, plus a decoder for the chained-fixup import table. Mach-O `S_SYMBOL_STUBS` entries are import stubs, never relocations.
- **2.3** `LoaderWrite{place, width, kind}` values are in link coordinates and composed in the order the loader applies them. Two words written at one place: the last write wins. Overlapping writes and gaps become Unknown. One reader, `stated_word`, replaces the raw reads in `records.rs`, `pointer_table` and `text_at`.
- **2.4** "Immutable after load" is decided by the container: a non-writable segment, PT_GNU_RELRO, or a Mach-O `SG_READ_ONLY` segment. A dispatch table in writable memory is refused, and a table's arms must lie in a code section.
- **2.5** Section roles come from the container's own statements. `holds_static_data` excludes the loader's meta
tests: Test runs are in a dedicated target directory, /home/user/wt-target/p2-container, with CARGO_INCREMENTAL=0. The shared directory kept serving other worktrees' stale builds of r2abi and r2s.

Final run: `cargo test -p r2abi -p r2image -p r2source -p r2engine -p r2s --all-features --no-fail-fast` — 485 passed, 0 failed. Clippy with `-D warnings` is clean on all five crates, and `cargo fmt` is clean.

Each spec'd test is in place and, where checked, fails on the old code:
- `ir` on rv_O0g has 13 ro
not_done: ['Only discovery carries a Confidence. Names, SourceFunctionInterface and PreparedFunctionCertificates do not yet; those seams belong to the p3/p1 tracks this round. Every premise set is empty today.', "`pdd` does not yet show the platform choice: a `FILE *` parameter leaves the whole callee signature untyped in native.rs DeclaredTypes (p3-decl's seam). So `__fgets_chk` renders `uint64_t (uint64_t x4)` on both the glibc and the bionic fixture. The selection is proven at the engine level (`NativeTar", 'musl gets no table of its own: evidence naming musl selects Unknown, the portable table only. It therefore loses `_Exit`, `exit` and `__libc_start_main` declarations it previously got by being treated as glibc.', 'Prototype lookup for a Mach-O function defined in the binary uses its linked name (`_main`). Mach-O DWARF lives in a dSYM that r2image does not read, so no declaration reaches it today either way.', "A lone import stub right after PLT0's multi-byte nop starts 4 bytes early: 7786-utf16le `wprintf` at 0x4003ec (radare2: 0x4003f0). This is older than this track; the new `is` layout exposes it.", "The bionic table leaves out `__read_chk`, `__fwrite_chk` and radare2's `android_log_*` and `xalloc_die` records (see README). The four string `_chk` functions trufae proposes (stpcpy, stpncpy, strlcpy, strlcat) are not added.", "The whole-binary coverage sweep (`tests/coverage/run_coverage.sh`) was not run: it needs a release build in the worktree's target directory and arm64 cross builds, with about 5 GB of disk free.", '`scripts/structure-report.sh` was not run over the whole workspace; that run fills the disk here. The same lint set was counted over the touched crates instead, at base and head.', 'Nothing was run on `../radare2`; this track does not touch it.']
##### impl:core-lowering
branch: track/core-lowering
summary: Both defects are fixed at their origin in r2dec lowering. All four listed equivalence keys moved from differs to equal, and the ratchet held on every function whose rendering changed.

**Fix 1 (N2): the INSERT lane mask** (commit 1a37f46d, crates/r2dec/src/fold/op_lower/implementation.rs)
- **Defect:** the mask was spelled `~(lane_t)0U`. For a lane narrower than `int`, C promotes before `~` applies, so the result is the int -1. Widened to the root that is all ones, and every root bit above the lane was erased.
- **Fix:** the lane's all-ones is now `(lane_t)-1`. C defines that as 2^w - 1 at every width, with no promotion. The cost is O(1) per INSERT.
- **New interpreter (test-only):** crates/r2dec/src/c_semantics.rs evaluates the rendered C tree under C11 integer rules: promotion, the usual arithmetic conversions, conversions, shifts, and literals typed as the emitter prints them. It reports undefined behaviour and refuses anything it does not model.
- **Matrix test:** `an_inserted_lane_keeps_every_root_bit_outside_it` (crates/r2dec/src/fold/tests/pipeline.rs) renders INSERT through the whole r2dec pipeline for 82 cases: roots of 16/32/64/128 bits, lanes of 8/16/32/64 bits, every byte position. It checks each against P-code INSERT on 3 input pairs.
- **Real-bytes test:** `a_write_to_part_of_a_register_keeps_the_rest_of_it` (crates/r2engine/tests/native.rs) compiles and runs renderings of real x86 code with cc: `xor al,0xff` at -O0 (siphash's `xor dl,0xff` shape), `mov ah,dl`, 
tests: - **r2dec:** `cargo test -p r2dec --all-features --no-fail-fast`: lib 336 passed, 0 failed; tests/architecture 2 passed. After the final test restructure the lib rerun printed 336 passed.
- **r2engine:** `cargo test -p r2engine --all-features --no-fail-fast`: lib 115, architecture 4, arm 9, body 2, control 4, degraded 2, discovery 13, imports 2, legacy_route_interlock 1, listing 18, machine 1, native 46, oracle 2, ranges 2, references 10, rendered 5, reuse 12, signature 2, start 3 all passed. Do
not_done: ['The Dylint the triage proposed (reject emitted C applying `~` or `-` to an operand narrower than int) was not added. Dylints lint the Rust source, not the C it emits, so they cannot see the type of an emitted operand. The term path already records `promoted()` for Negate/BitwiseNot in r2rewrite type', 'The exhaustive 16/32/64/128 × 8..64 × byte-position harness interprets the rendered AST under a Rust model of C11. It does not compile it. Real-compiler checks exist only for the x86-reachable shapes, in the r2engine native tests. The interpreter was cross-checked against the old bug, which it repro', "The single total helper r2sleigh_insert_uN from r2rewrite's typed spelling (P10) was not done. The mask is still spelled inline in the r2dec statement lowering.", "The origin of N5 (F1: the reload certificate follows SEXT, so RAX_2 gets the Parameter binding) is untouched and belongs to p1-identity. typed_compete is now equal because the value is spelled through the binding's signed int32_t declaration and widened, not because the binding is right.", "Found out of scope, not fixed: `mov rax,rdi; mov ax,si; ret` renders `uint16_t replace_low_word(uint64_t RDI_0, uint16_t SI_0) { return SI_0; }`. The result width is narrowed by recover_interface's syntactic INSERT-at-position-0 rule (owner r2ssa / PE track). My test adds `not rax` to avoid it.", 'Not run: the whole-workspace test suite, certify_render.py, the coverage sweep and diff_r2.py (left to the integrator per the track instructions).', 'Not done: an equivalence-gate run over all 756 functions. The gate ran on the 55 whose rendering changed, as found by the full old-vs-new pddj census; the other 701 print byte-identical code.']
##### impl:core-ssa-entry
branch: track/core-ssa-entry
summary: N4 is fixed where it starts, in r2ssa's CFG and SSA construction. The branch sits on engine/review-fixes-vfmgfv at 3fbea1dc (ancestry confirmed). The worktree is now detached at 41dd7a13. Nothing was pushed.

**The fix (0254c670).** The invariant is that the root of a function's graph has no predecessor.
- **Where it lives:** `CFG::rebuild_edges` in crates/r2ssa/src/cfg.rs. When some block branches back to the entry, it puts an empty block in front of the entry, keyed `cfg::ENTRY_EDGE = u64::MAX`. Its only successor is the entry. The CFG refuses any lifted block that claims that key.
- **Why this is enough:** this is Cytron's entry node. The entry becomes an ordinary merge. The existing frontier and placement rule (a block needs 2 or more predecessors) is then correct unchanged. Renaming gives the entry edge version 0.
- **Complexity:** one extra predecessor query when the graph is built. Dominators and phi placement cost the same as before.

**Entry versus root.** `SSAFunction::entry` still holds the address that names the function. The new `SSAFunction::root()` and `RewrittenFunction::root()` return the block control enters by. I checked every place `entry` is read. The ones that seed an entry state, start a walk, or place an entry definition now use the root:
- SCCP's pseudo-edge and its reachability pruning. Without this, pruning deleted the entry-edge block and its phi operands.
- Control-domain seeding.
- The backward walk in boundaries.rs that looks for the entry value
tests: **Behaviour tests (crates/r2engine/tests/native.rs).** Each test renders a function from bytes, then compiles and runs the C against a reference implementation. I confirmed all of them fail on the old code.
- `a_loop_at_the_entry_carries_what_its_latch_writes`: the old code rendered a loop that never terminates. `run_rendered` now has a 30-second SIGALRM watchdog, so this shows up as `check None failed` instead of hanging the suite.
- `an_entry_with_two_latches_merges_the_caller_and_both_latches
not_done: ["Coverage sweep (tests/coverage/run_coverage.sh) not run. It does a release build in the repo's own target/ and uses Darwin-only `clang -arch`. Disk was at 4.8G free. The certification gate and the full equivalence gate were run instead.", 'diff_r2.py not run. Discovery, naming, decoding and cross-references are untouched, and afi output is identical before and after.', "promote.rs is unchanged. The block-0 premise is enforced from function/build.rs using the CFG. When P4's frame partition owns promotion, the premise should move with it. Also seen in passing: promote.rs takes `blocks.first()` as the entry, while the CFG uses the source's declared entry, which differ", 'Merge risk with p1-identity: function/rewrite.rs has a one-token change in two places, `insert_ops(self.entry, ...)` becoming `self.root()` in the entry-lane and boundary minting. Any new code in other tracks that uses `function.entry` as a walk root or entry-state seed needs `root()` instead. For f', 'Phi placement at the entry is not pruned (flag and temp merges appear there), the same as at every other loop header today. Dead-merge removal handles them afterwards. Nothing was changed here.', 'Private target dir /home/user/wt-target/core-ssa-entry was deleted at the end because disk was below 5G. The before/after r2s binaries are kept at scratchpad/r2s-core-ssa-entry-before and r2s-core-ssa-entry-v2.']
##### review:core-lowering
verdict: Nearly mergeable, with one blocking ownership defect. Both fixes are correct, and I verified them myself. Built at cadc7dea in a private target dir (worktree and target dir removed afterwards). (1) All four new behaviour tests pass at the tip. Reverting each fix makes its test fail: the matrix test got Ok(239) where Ok(13039) was expected; flip_low_byte and typed_compete each failed check 1. (2) I also did the check the task asked for and the track skipped: I compiled all 82 printed matrix renderings, moved to a page mapped at 0x40003000, and compared each against P-code INSERT on the three vectors. All 164 gcc -O0/-O2 runs with -fsanitize=undefined -fno-sanitize-recover passed, and all 164 
 - BLOCK: The new `type_fact_of_width` (lines ~1022-1035) makes r2dec a second owner of the fact 'this C type describes this storage width'. r2types already owns that fact in `r2types::admissible_declaration_type`, whose doc is 'Admit a logical type only where it describes this exact storage width', and in `parameter_declaration(slot, width_bits)`. This crate already defers to it for the same question at binding_plan/rules.rs:1283 (`r2types::admit_declaration_type`). The hand-rolled rule, `declaration_typ | FIX: Replace the body of `type_fact_of_width` with `r2types::admissible_declaration_type(ty, width_bytes * 8, self.pointer_bits())`. Keep the `refusal_evidence!` trace for the None case. Better still, apply it to each candidate in `type_hint_for_var` before the candidates-agree vote: a candidate of the w
##### impl:core-lift
branch: track/core-lift
summary: Track core-lift is on branch track/core-lift, based on engine/review-fixes-vfmgfv at 1e53fc79 (`merge-base --is-ancestor 3fbea1dc` checked). HEAD is detached at the end. The fix is in r2sleigh-lift only; r2il is unchanged, because no new IL construct was needed. The only other file changed is crates/r2engine/tests/native.rs, which gets tests only. None of the 12 target keys moves to `equal` on this branch alone: `mem_scan2` needs track/core-lowering merged, and `value_count_bits` needs a renderer for the `Lzcount` op.

**NEW-1: loops inside one instruction are no longer lifted as Unimplemented.** The work is in `crates/r2sleigh-lift/src/internal_control.rs`, new function `resolve_local_loops`, which runs whenever an instruction has a local branch back. It tries two things in turn:
1. **Loops decided by constants** (`internal_control/unroll.rs`). The p-code is run in order over the values that are constants, computed with `r2il::eval::apply`, and every branch whose condition is a constant is decided. The loop is repeated exactly as many times as the p-code repeats it. Data-dependent skips inside a pass stay as forward skips for the existing select rewrite, and anything written inside a skip becomes unknown. The walk gives up (and the instruction refuses as before) on:
   - a branch back that depends on data;
   - a branch back reached on only some paths;
   - a skip that would leave its pass;
   - more than 16384 steps. This is an explicit budget with refusal; the longest real
tests: Printed counts, per crate touched (not the whole workspace):
- **r2sleigh-lift** (`cargo test -p r2sleigh-lift --all-features --no-fail-fast`): unit tests 92 passed, 3 ignored. Integration tests 14 passed: exclusive_access 1, fmadd_operands 1, neon_lanes 3, neon_rev64 2, packed_extension 2, repeated_string 1, thumb_mode 3, word_shuffle 1. Doc tests: 5 ignored. 0 failed.
- **r2engine** (`cargo test -p r2engine --all-features --no-fail-fast`): 253 passed, 0 failed. By target: lib 115, architecture
not_done: ['No value_count_bits key reaches equal on this branch: BSR is lifted as the canonical Lzcount, which nothing downstream projects or renders (r2ssa machine lowering, r2rewrite typed, r2dec); suggested task task_c39c5ef7. PopCount-only spelling experiment (not committed) made all 6 equal.', "mem_scan2 clang-O2 is a ratchet 'new differs' on this branch alone (exposed INSERT-mask promotion bug); it is equal only once track/core-lowering (1a37f46d) is merged - merge order dependency for the integrator.", 'Kani harnesses for the three counting lemmas (scan.rs kani_proofs) are written but NOT run: cargo-kani is not installed here and disk was too tight to install it.', "Hardware behaviour for BSF/BSR 32-bit form with zero source (whether upper 32 bits of the 64-bit register are preserved) is a documented judgement (AMD 'unchanged'), not verified on hardware in this session.", 'ARM NEON vld1/vst1 and AArch64 MTE p-code loops go through the same constant walk but have no dedicated test; only x86 instances are tested.', 'CMPXCHG instruction-local diamond still Unimplemented (and the existing forward-skip rewrite drops its store); out of scope, suggested task task_716ae65d.', 'Whole-workspace test suite not run (per instructions; integrator runs it); certify_render/coverage sweep not run.']
##### fix:core-lowering
branch: track/core-lowering
summary: The blocking finding is fixed. Two of the non-blocking findings are also done: the compiled INSERT harness, with c_semantics deleted, and a Linux gate on the fixed-address test. No rendering in the corpus changed: a pddj census of all 756 gate functions is byte-identical between the previous HEAD's r2s and the new one, including kind, refusal and every proof counter. The 55-key equivalence gate held the ratchet.

**Blocking fix: r2types is the only owner of "this type describes this width"** (commit 64458558, in `crates/r2dec/src/fold/op_lower/implementation.rs` and `memory_renderer.rs`)
- `type_fact_of_width` now asks `r2types::admissible_declaration_type(ty, width_bits, ptr_bits)` and takes the canonical type it returns. When the answer is None it still writes the `type-fact-width` trace, and it clones the type only when tracing is on.
- `type_hint_for_var` now checks each candidate's width before the vote. A candidate of the wrong width can no longer be picked or block a candidate of the right width, and the vote compares the canonical spellings.
- **Where I did not follow the review:** the review said to keep the `memory_renderer` check on `declaration_type_width_bits`. I changed it, because that function has no width for an `Int` of unknown signedness, which the admission rule now accepts. Kept as it was, the renderer would have refused a type that type_hint_for_var had just accepted. The check now passes an element when it is the operation's own machine type from `uint_
tests: - **r2dec** (`cargo test -p r2dec --all-features --no-fail-fast`): lib 335 passed, 0 failed. That is 336 minus the deleted c_semantics self-test. architecture 2 passed; doc tests 1 passed, 1 ignored.
- **r2engine** (`cargo test -p r2engine --all-features --no-fail-fast`): everything passed: lib 115, architecture 4, arm 9, body 2, control 4, degraded 2, discovery 13, imports 2, legacy_route_interlock 1, listing 18, machine 1, native 46, oracle 2, ranges 2, references 10, rendered 5, reuse 12, sig
not_done: ["Non-blocking #6, re-blessing baseline.json: the four target keys are still recorded as `differs`, with causes describing the bugs that are now fixed. My edit setting them to equal was denied by the permission classifier as a change to a shared resource (the README says blessing is the integrator's j", 'No test for the ownership fix (644585584) fails on the old code. The full census and a pipeline probe found no reachable input where the old and new votes spell a type differently, so the change is behaviour-neutral on everything I could reach.', 'Found, not fixed (r2types): `declaration_type_width_bits` gives no width for an `Int` of unknown signedness, while `admissible_declaration_type` accepts it. That is an inconsistency inside the owner. I worked around it in the memory renderer instead of changing r2types, because the seal (p1-reads) a', 'Found, not fixed (binding plan / p1-reads): a signature parameter typed as an int of unknown signedness makes the whole function refuse with `missing_program_variable_authorization`.', 'Non-blocking #4 (count type-width mismatches in pddj) was not done: they are traced only, and the wording now says so.', "Non-blocking #5 was not done: the route-independent 'the rendered access moves the operation's bytes' check for the SlotName, SlotMember, PtrMember and ParamArray routes. It belongs to P10 or the binding plan.", 'The INSERT matrix and the typed_compete store test now run only on Linux (fixed-address mapping). They do not run on macOS.', 'Not run: the whole-workspace test suite, certify_render.py, the coverage sweep and diff_r2.py (left to the integrator). The equivalence gate was not run over all 756 functions; renderings are byte-identical, so the verdicts cannot move.', 'My private target dir /home/user/wt-target/core-lowering was deleted because free disk space was below 5G (4.2G left). Any fix round rebuilds from scratch.']
