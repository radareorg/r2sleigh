# #26708 Run plugin post-analysis hooks from aa and aaa with a depth ##analysis: close (short)

## Close note (post on #26708, then close)

Closing this. It existed so the r2sleigh analysis plugin could run in `aa`/`aaa`. That plugin no longer exists: r2sleigh is now a standalone engine that uses radare2 only as a reference to diff against. No in-tree plugin implements `post_analysis`, so nothing would read the depth. Fixing the three blockers from your review would therefore help no user: plugins reachable from `aa`/`aaa` without opting in, `post` running before the aaaaa/noreturn refinement, and a depth that exists only around post dispatch. If a real consumer appears, I will open an issue built on your pass descriptor (id, requires/provides, before/after, eligible, run, scope, preset membership), with a tracing test plugin, instead of reviving this.

## Publish (not posted)

There is nothing to push. The PR head is 0verflowme:pr/post-analysis-depth bc38668. Its commit 86e4cffea1 is already left off the rebuilt local `anal/subregister-argument-spills` (cba6b13382). The fork branch stays, because this session cannot push to or delete it on the fork.

Post the close note on #26708, then close #26708 without merging. `gh` is not installed here, so use the web UI.
