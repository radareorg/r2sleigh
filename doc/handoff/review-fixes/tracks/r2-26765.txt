PR radareorg/radare2#26765 'Read registers and target details from lldb's debugserver' (fork branch head 2774ebf8; fetch it from the fork remote). trufae's COMMENTED review (2026-09-21 00:09) asks 9 things (listed in the triage). Rework on branch 'debug/gdbr-lldb-capabilities-v2' from origin/master + the PR's commits: model LLDB as a capability profile of ONE gdbr implementation - a capability record on libgdbr_t (has_g, has_pN, thread_suffix, has_qProcessInfo) probed at connect (never assumed from 'is lldb'); one read_registers() using 'g' when it works else a per-register pN loop sharing the packet I/O helper; one stop-reply parser filling the register cache in place (no strdup of every stop packet; single path used by gdbr_stop_reason, gdbr_attach, send_vcont); qProcessInfo sent once and only when advertised or the stub is lldb; checked buffer growth (refuse when len > SIZE_MAX/2 or exceeds data_max's type; grow to next_pow2(len+1)); the Darwin CPU-type map moved to a reusable place (or target.xml); gdbr_read_process_info called once. Add protocol-level unit tests in test/unit driving gdbr against a scripted packet transport (transcript replay): normal gdbserver still uses g, LLDB with and without g, pN fallback, stop reply with registers, thread suffix on/off, bit-sized offsets, oversized response refused, attach T reply. No Apple hardware here: say explicitly what needs the user's Mac to validate (Rosetta/debugserver runtime). Draft superseding PR text and replies to each of the 9 points.
