Plan P1.3, P1.4, P1.5 (owners: r2dec binding_plan seal, r2dec ledger/effect ledger, r2abi platform data, r2source SourceCallEffect, r2ssa machine_context/rename, r2sleigh-lift register naming).
1) Reaching-values validator in crates/r2dec/src/binding_plan/seal.rs replacing the self-confirming re-run (~29-50, ~131-136): an independent check that every rendered read of a C variable sees exactly the SSA value it stands for; equality is syntactic SSA identity (never same_content); AST markers for elided definitions; iterate round-robin in reverse postorder (Kam-Ullman d+2 passes, argue termination). On a stale read of value v through binding B: evict v from B's class (own binding + explicit copy), rebuild only B's component, re-validate; the partition only gets finer so this terminates; count each in proof 'split'. Residual only if a read cannot be split.
2) Definite assignment keyed on SSA versions: delete ElisionReason::CallClobberedDeclaration (ledger.rs ~156-169, effect_ledger.rs ~155-167), the sealing.rs ~617-622 exemption, CallClobbered in binding_is_entry_declared, and the flow-insensitive 'written' excuse (r2dec/src/lib.rs ~582). A read of a version-0 register that is neither a parameter nor callee-preserved (preserved registers ARE held-from-entry: saving rbx must NOT become a residual), or of an unclaimed CallDefine, becomes the residual form. Today P0 made 'every function that saves a callee-saved register trap on its first statement' - fix that at the origin: preserved-register saves are held-from-entry values restored before return (prove it via the convention's preserved set), not residuals.
3) SystemReserved register class: new r2abi platform table with psABI citations: x86-64 SysV FS base, i386 GS, AArch64 TPIDR_EL0, Darwin arm64 x18, MXCSR/x87 control bits; cc sdb files stay untouched. SourceCallEffect becomes three-valued (r2source contracts.rs ~3769-3845): Clobbered / Preserved / SystemReserved(invariant across calls). machine_context.rs ~1010 and rename.rs ~984 emit no CallDefine for SystemReserved registers; map the Sleigh FS_OFFSET name in r2sleigh-lift register naming so both canary loads read FS_OFFSET_0.
Tests that must fail on HEAD: an injected stale read is split (unit test in seal.rs); rv_O0g/rv_O2 main: both canary loads read the same entry value (no FS_OFFSET_15 declared-but-unassigned); a function that pushes/pops rbx renders without residual; rv_O2 main's XMM0_4 (avg result read) is a residual until call contracts land; pddj of every function in rv_O0g and rv_O2 compiles with -Werror=uninitialized -Wmaybe-uninitialized at -O2 (gcc) and has no indeterminate read; the equivalence gate moves no function away from equal and turns 'uninit' records to equal/residual.
