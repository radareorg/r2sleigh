Plan P2 (owners r2abi statement types, r2image loaders, r2source Confidence, r2engine consumers, r2s spelling). Evidence: journal labels critique:commands, critique:data-objects, trace:r2-prs (r2abi vendored tables finding).
2.0 Move Confidence (today only in r2engine/src/discovery.rs) into r2source with premises: BTreeSet<Premise{ClosedWorld, UbFreeSource}>; names/interfaces/certificates carry it (later phases fill premises).
2.1 FIRST: move container statement types (Section, Segment, Symbol, Relocation, Entry, LoaderWrite) into a new module r2abi::statement; delete the duplicates in crates/r2engine/src/program/source.rs (~33-176) and the field-by-field copy in crates/r2s/src/session.rs (~95-159); r2image produces them directly. (P0 added Container.segments/Segment/Permissions: fold them into this.)
2.2 One relocation-record reader (merge r2image lib.rs ~787, loader/elf.rs sectioned + tabled); identity = (table file offset, index) (keeps .rela.iplt); Mach-O S_SYMBOL_STUBS entries become import_stubs, not relocations; add a chained-fixup import-table decoder for bind names.
2.3 LoaderWrite{place, width, kind: Relative|Absolute|Preemptible|Resolver|NotAnAddress, value} in link coordinates (never add baddr; per-format rules in the critique), composed in loader order; one reader stated_word(place, width) replacing raw reads in r2engine query/records.rs, native.rs pointer_table and native.rs text_at.
2.4 Immutable-after-load ranges on Container: non-writable PT_LOAD ∪ PT_GNU_RELRO ∪ Mach-O initprot without W / SG_READ_ONLY, minus loader writes unless read-as-loaded; consume P0's TableBytes::Unsealed hook: a table in writable memory is refused; table targets must lie in a code section.
2.5 SectionRole from container statements (SHT_*, SHF_MERGE|STRINGS, PT_INTERP, eh_frame, S_CSTRING_LITERALS); holds_static_data excludes loader metadata (strtab, dynsym, rela, note, gnu_hash, dynamic, versym).
2.6 Command facts (radare2 spelling; verify against radare2 source in /home/user/radare2 libr/core/cbin.c): baddr presentation only (ELF lowest PT_LOAD vaddr: 0x400000 non-PIE; Mach-O 0x100000000); iS perms from segment ('----' for unloaded), Mach-O 'segment.section' identity; is gets bind/visibility/imports (incl stripped dynamic imports); ir gets type/paddr/addend and all 13 relocations on rv_O0g (4 RELATIVE); ie every entry kind (init/fini arrays); stub flag sizes from structural stub detection replacing the name gate (naming.rs ~435); iz lists data-section strings only with escapes (\n) and izz scans everything (full proven-string typing comes in P8 - do the section-role part now).
2.7 Platform evidence: r2image derives libc evidence (PT_INTERP ld-linux/linker64/ld-musl, .note.android.ident, EI_OSABI); r2engine selects platform, Unknown => portable table only; r2abi records scoped per libc (glibc, bionic, darwin; add Platform::Android); r2image strips Mach-O leading-underscore decoration once; Prototypes::get becomes an exact lookup; crates/r2abi/data/README.md states the tables are r2abi's own keyed by C identifier and lists deltas from upstream (radare2 #26743 is being reworked upstream to keep normalized keys: document the mapping).
Tests: ir on rv_O0g has 13 rows incl 4 RELATIVE with type column; PIE at non-zero base RELATIVE value == addend; i~baddr 0x100000000 (tests/fixtures Mach-O) and 0x400000 (non-PIE build); axt 0x2008 on rv_O0g shows 0x4018 (data->data via stated value); iz has no .dynstr/NOTE/eh_frame entries and prints '%d %s\n' escaped; a dispatch table in writable memory is refused; Android PT_INTERP selects Bionic __fgets_chk order. Run scripts/diff_r2.py against a radare2 build: PATH=/home/user/r2prefix/bin (built from the radare2 base branch) on tests/fixtures and /home/user/radare2-testbins/elf (limit 30), judge each disagreement (radare2 may be wrong), record results.
