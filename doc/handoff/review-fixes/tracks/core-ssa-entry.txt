Root causes found by the equivalence triage of the P0 census (tests/equiv/baseline.json names every record's cause). Fix each at its origin, prove it with a behavior-level test that fails on the old code, and show the listed equivalence keys moving to 'equal' (tests/equiv/run_equiv.py --r2s <your r2s> --baseline tests/equiv/baseline.json --only '<regex of keys>'; the ratchet must hold).

Owner r2ssa SSA construction (domtree/phi/rename). A function whose entry block is also a branch target (a loop back to the entry) must have phis at the entry joining the implicit entry definition and the back edge; this is the classic 'virtual entry' construction (add a synthetic predecessor for the entry, or treat entry as having an implicit incoming edge in dominance frontiers and renaming). Audit every consumer that assumes the entry block has no predecessors.
- N4: SSA construction ignores the implicit entry edge, so an entry block that is a branch target gets no phi (or a phi without the entry operand)
  owner: r2ssa (domtree.rs dominance frontier, phi.rs placement, rename.rs phi operands)
  evidence: VERIFIED. crates/r2ssa/src/domtree.rs:210-229 adds frontier edges only `if preds.len() >= 2` (:213), and the entry dominates itself (:65). crates/r2ssa/src/phi.rs:135 repeats the >= 2 filter at placement. Phi operands are one per CFG predecessor, and no edge supplies version 0. So when the entry block is also a loop header, it has one CFG predecessor and gets no phi. This happens with mutual tail recursion merged into one body (even `jmp odd`, odd `jmp even`) and with any function whose first block is a loop. Every use in the entry then reads the entry value, and the loop-carried definitions in the latch are unobserved and elided. Re-run pdim, clang-O1 shape_mutual_even: `Block 0x401710: preds: 0x401745; tmp:70500_1 = RDI_0 & RDI_0`, with RSI_2 and RDI_2 'elided UnobservedValue'. gcc-O2: `Block 0x401ab0: preds: 0x401a98` and `Block 0x401a80: preds: 0x401ac8`. Reproduced (scratchpad/verify/n4/t.S): `entry_loop: add rax,rdi; dec rdi; jnz entry_loop; ret` renders `for(;;){ RAX_0 += RDI_0; if (RDI_0 == 1) break; }` with 0 refused. The decrement is lost, and the loop never terminates for RDI != 1. A second repro with two back edges into the entry (t2.S) gets entry phis with no entry operand, `RDI_1 = phi([0x40110b]: RDI_2, [0x401113]: RDI_2)`, which loses RDI_0 entirely; that render fails closed with 'read_before_assignment'.
  fix (triage proposal; challenge it): Give the CFG a virtual entry node, or equivalently split the entry block with an empty synthetic pre-entry block whenever the real entry has any CFG predecessor. Build dominators, frontiers, phi placement and renaming over that graph. Entry values are definitions on the virtual edge, so the real entry is a join whenever it has a CFG predecessor. Phis are then placed there by the ordinary iterated-frontier rule, with version 0 as the virtual edge's operand. Consumers that assume 'the entry has no predecessors' must read the virtual edge instead: interface recovery's read-before-write, the promote.rs block-0 rules (delta and entry_adjustment), liveness and boundaries. Tests (crates/r2engine/tests/native.rs): the one-latch repro must render a loop that decrements RDI and carries RAX; the two-latch repro must render with a three-operand phi; and the two-function mutual tail recursion must render both steps. Complexity unchanged: O(E·α) dominators and O(E+V) placement.
  equivalence keys: tests/corpus/shapes.c::clang-O1::shape_mutual_even, tests/corpus/shapes.c::clang-O1::shape_mutual_odd, tests/corpus/shapes.c::clang-O2::shape_mutual_even, tests/corpus/shapes.c::clang-O2::shape_mutual_odd, tests/corpus/shapes.c::gcc-O2::shape_mutual_even, tests/corpus/shapes.c::gcc-O2::shape_mutual_odd