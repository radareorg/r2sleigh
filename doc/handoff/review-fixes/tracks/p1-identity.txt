Plan P1.1, P1.2, P1.6, P1.7, P1.8 (owner r2ssa; consumers in r2types/r2dec re-checked).
1) ValueView: one bit-identity fact view(v) = (root, prefix_bits, ext in {Exact, Zero, Sign, Unknown}), computed optimistically over SCCs in O(V+E) (lattice height 2, no iteration cap). It REPLACES the identity rules of canonical_value_roots (crates/r2ssa/src/function/rewrite.rs ~785-826) and adapt_root_width (function/mod.rs ~3940): SUBPIECE at offset>0, size-changing Cast, extension-then-truncation are never 'the same bits'. same_bits, copy_class (replacing predicates.rs collect_compare_copy_sources/canonical_compare_operand and raw ValueId lookups in values.rs; delete the shared.rs ~1593 or_else(root_of) workaround), constant::root_of (Zero/Exact chains) and address provenance are projections of this one fact. A constant lane (SUBPIECE(c,k)) comes only from the constant folder as (c>>8k)&mask. The stack-reload certificate (semantic/certificates/stack.rs ~1207-1315; mod.rs ~1271) gets relation {Identity, Derived} from the view; only Identity enters reload_values, Parameter home membership (r2types function_facts/mod.rs ~2451), return carriers. Delete the depth>8 recursions in r2types prepared.rs (~1431, ~1535) by answering address provenance from the view. Re-check all ~19 consumers of canonical_value_roots (incl. call targets function/mod.rs ~1421/1474/1529, boundaries.rs:69, interproc/mod.rs ~2675, signature_infer.rs ~236, returns.rs ~519).
2) Liveness (liveness.rs): same_content reads the view (lane/entry unions follow the lifter's lane-to-byte mapping); same_content_reads keyed on (object, offset, width, reaching memory version set) valid across blocks; semantic/objects.rs ~106: an uncertified Call/CallInd clobbers the escaped-unknown object. Perf: construction.rs keeps absorbed liveness (small-to-large merge), first_interference as a start-sorted sweep.
3) Parameter homes by dataflow: drop the '||' register-offset clause in promote.rs (~36-70) so 'call f; mov [fp-4],eax' is not a parameter home; function/build.rs ~146-162 passes only argument carriers (not the return register).
4) Unspecified(width) leaf: mint_entry_lane_projections (rewrite.rs ~243, ~405-470) builds INSERT(Unspecified, formal, off) for partial entry-lane writes; values.rs treats Unspecified as top; r2dec renders a read of Unspecified bits as a residual (never an indeterminate C variable).
5) Callee-reach soundness (interproc/mod.rs): a formal-dependence bitset decides poisoning; an Unknown access poisons only formals its address depends on; unknown call poisons only formal-derived pointer args; constant-based address classified Global.
Tests that must fail on HEAD: SUBPIECE(SEXT(t),8) not Identity; SUBPIECE(0x1122334455667788,4) never resolves to 0x55667788; AH read of a phi of constants does not resolve to the low byte; rv_O0g mul_div 'equal' in the equivalence gate (and on inputs (100,3),(-100,3),(INT64_MAX,7),(7,-2),(-9,-4) via a compiled harness); 'call f; mov [fp-4],eax' not a parameter home; rv_O2 rotl listing no longer claims 'defines rcx in [0x0, 0xff]'.
