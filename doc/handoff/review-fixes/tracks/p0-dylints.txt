Plan P0.8: revive the Dylint suite in tools/dylints/r2sleigh_lints. ROADMAP standing debt: 32 file-scanning tests assert on functions renamed/deleted in Aug-Sep; lints written for plugin-side policy can no longer fire. Install the toolchain (rustup nightly as pinned by tools/dylints rust-toolchain, cargo install cargo-dylint dylint-link) if possible in this container. Fix or delete each rotted test so it asserts the invariant on current code (not the old function names); delete lints that can only fire on deleted plugin code (and their docs), keeping every lint whose invariant still applies; add a CI job (.github/workflows) that runs the Dylint tests and 'cargo dylint --all' in strict mode on the slices already clean, and wire scripts/quality-gate.sh --strict-dylint. Report which lints were kept/deleted and why, and current finding counts. Also add lints that encode repeated failures found in this review where cheap and precise: (a) no format!("fcn outside crates/r2engine/src/names.rs, (b) no 'sla_struct_' string literal outside r2types naming, (c) r2dec may not call canonical_value_roots-style root lookups directly (only r2ssa views). If a lint needs code that another track is rewriting, write it against the current API and note it.
