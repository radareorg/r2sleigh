PR radareorg/radare2#26743 'Declare the fortified libc functions under their identifiers' (merge conflict; stale). Read trufae's two issue comments of 2026-09-21 fully. Rebuild it from origin/master as trufae's 5-patch series on branch 'types/fortify-chk-v2': P1 move the 8 records (memcpy/memmove/memset/strncpy/strcat/strncat/vsprintf/vsnprintf _chk) to the generic table WITHOUT renaming keys (+66/-66 as trufae sketched), keep longjmp_chk linux-scoped, drop the blank tail in types-linux; P2 Bionic fgets_chk/fread_chk (Bionic argument order: fgets(char*,int,FILE*,size_t), fread(void*,size_t,size_t,FILE*,size_t)) in types-android.sdb.txt; P3 the four shared string _chk (stpcpy/stpncpy/strlcpy/strlcat); P4 move clean_function_name() into the dll_stripped branch of libr/util/utype.c (~1164) fixing 'sub.___memcpy_chk_123' resolving to memcpy_chk; P5 grouped r2r tests plus test-local 'tn~0x~$' in test/db/anal/noreturn. Verify each claim trufae makes (e.g. that type_func_try_guess/trim_lodashes already resolve sym.imp.__memcpy_chk on Linux) by running r2 on a fortified binary (build one: gcc -O2 -D_FORTIFY_SOURCE=2 using memcpy/strcpy/fgets/printf into /home/user/r2-pr-drafts/fixtures). Draft: a superseding PR description and a reply to trufae agreeing point by point where correct and stating evidence where not.
