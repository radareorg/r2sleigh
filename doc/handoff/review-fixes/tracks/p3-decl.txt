Plan P3 (owners: r2image debug.rs, r2abi TypeGraph, r2source SourceTypeGraph, r2engine wiring). Evidence: journal labels critique:memory-model (DWARF findings, NEW findings on name-keyed prototypes and fbreg formals), critique:binding F7, critique:data-objects (DWARF globals/layouts), critique:calls F2/F3.
3.1 Rewrite crates/r2image/src/debug.rs: memoise nodes by (unit, DIE offset) (delete SPELLING_DEPTH); read every attribute through a merged view following DW_AT_abstract_origin and DW_AT_specification; accept UnitRef/DebugInfoRef/ref_sig8; node kinds: struct/class members at stated offsets, bitfields, arrays with counts (flexible = none), unions, enums (as underlying int), Code{signature, variadic}, Opaque{tag} for incomplete types, qualifiers as bits incl volatile; refusal per node (never the whole graph); read CU-scope and function-scope variables (DW_OP_addr/addrx) with types; lexical-block pc ranges; DW_OP_fbreg formals and locals; key subprograms by low_pc and DW_AT_ranges; arrays decay to pointers only in formal position.
3.2 r2abi TypeGraph: one declaration model; sdb prototype spellings parse once into the same graph; a tag with no layout is Opaque.
3.3 SourceTypeGraph in crates/r2source/src/contracts.rs (~525-842): validate a stated layout for consistency (do not require natural alignment: packed and i386 layouts are valid), add Opaque{tag}, Array{count: None} (member/pointee only), Code{signature}, 80/96-bit floats, and a closure(roots) constructor that never refuses because a node is unreachable; an exact declared interface may not carry a None logical value.
3.4 r2engine: delete DeclaredTypes (native.rs ~1245-1283, intern ~1445-1475) - per-item interning so one unmappable node degrades only itself (fixes list_len's 'uint32_t c' + 'return (uint64_t)c'); keep the DWARF map keyed by ADDRESS, separate from the sdb name map (fix the static-name collision at native.rs ~728-731, ~1104-1108; ICF-folded subprograms refused unless structurally equal prototypes); declared_frame emits a typed ParameterHome for every fbreg formal; frame pointer's distance from entry from r2ssa's prologue proof (spill only cross-checks); restated(): declarations authoritative, graph by closure, O((S+D) log) merge, no Local declared at return-address or canary slot; one owner of the logical return (drop returns.rs ~319 fallback when a declaration exists). Globals: DWARF variable types flow to data object types (g_counter int, g_table int[16], g_msg const char*) so '2 data object types refused' disappears on rv_O0g.
Tests: r2image unit tests on tests/fixtures/rv_O0g: v extent 16 type Array(u32,4); struct node members at 0,8,16 with the next->node cycle; a constprop/.isra clone is read (build a fixture with gcc -O2 -g where a clone appears); Pointer(Opaque) accepted; a two-CU static-name collision fixture shows each unit's own prototype; SourceTypeGraph accepts i386 and packed layouts; rv_O0g list_len declares 'int32_t c' and ends 'return c;' (pdd); clang -O0 with DW_OP_reg6 frame base puts locals in entry coordinates. Keep a strip --strip-all twin assertion that nothing changes behaviour-wise when DWARF is absent (no panics, same refusal classes). Rendered-output changes are expected (more names/types); run the equivalence gate on review.c and confirm no function leaves 'equal'.
