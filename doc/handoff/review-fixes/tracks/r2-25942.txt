PR radareorg/radare2#25942 'Optimize /as syscall search candidate discovery' (fork branch codex/syscall-search-perf-followup? head 54b79883 - find it on the fork via git ls-remote). Two CHANGES_REQUESTED reviews; open author question (libflutter.so arm32 8 -> 34 hits). Diagnosis from triage: the extra hits are false positives because (1) the candidate domain is IO maps incl .rel.dyn inside the R-X PT_LOAD, (2) when the syscall-number register is unknown the code falls back to the svc immediate (wrong on ARM EABI where the number is in r7 and the immediate is 0; only OABI encodes 0x900000+nr). Rework on branch 'search/syscall-candidates-v2' from origin/master + the PR: take the number from the syscall register as proven by the transfer (r7 on ARM EABI, eax/rax on x86 syscall); the immediate only where the ABI puts it there (ARM OABI per e_flags: imm-0x900000; x86 'int' gives the vector not the number); otherwise refuse the hit; restrict candidates to executable sections (SHF_EXECINSTR) intersected with search.in boundaries when section headers exist, maps only without sections; move per-arch byte/mask pattern knowledge behind the arch plugin (RArchInfo or a plugin-provided syscall encoding table + code alignment) so core runs one generic aligned masked scan. Build arm32 EABI and arm64 test binaries if a cross toolchain exists (clang --target=armv7-linux-gnueabihf -ffreestanding -nostdlib + ld.lld works here) with known syscalls and a data word that looks like 'svc 0xc'; add r2r tests; measure hit counts and time before/after. Draft a reply answering the libflutter question with the diagnosis, and a superseding PR description.
