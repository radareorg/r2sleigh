radare2 work. Repo /home/user/radare2 (remotes: origin = radareorg/radare2, fork = 0verflowme/radare2 read-only). Existing worktrees under /home/user/r2wt; create yours with 'git -C /home/user/radare2 worktree add /home/user/r2wt/<name> -b <branch> origin/master' (fetch origin master first). Build into a private prefix (./configure --prefix=/home/user/r2prefix-<name> && make -j2 && make install; reuse ccache if present) and test with r2r (test bins at /home/user/radare2-testbins; symlink or use R2R_BINS as the existing worktrees do - look at /home/user/r2-pr-drafts/rebase-log.md and the previous track's notes for the working recipe). Disk is limited: delete your build objects (make clean) after the final test run if above 1GB.
GitHub: load tools via ToolSearch ('select:mcp__github__pull_request_read,mcp__github__get_file_contents') to read the PR, its reviews, review threads and comments on radareorg/radare2. Do NOT push, comment, close or resolve anything.
Commits: radare2 style, ONE line subject ending with a ##tag, no body unless needed, NO assistant attribution, NO Co-Authored-By.
The triage of the user's open PRs is in /root/.claude/projects/-home-user-r2sleigh/21f32657-b293-542a-abb6-f78a3cbef7ac/subagents/workflows/wf_00286c1c-05f/journal.jsonl (label trace:r2-prs). Write a CONCISE draft (radare2 maintainers prefer short, technical replies: at most ~12 lines per reply, no restating the review) at /home/user/r2-pr-drafts/<pr>.md: new PR title/description for a superseding PR opened from a radareorg branch (the fork cannot be pushed from this session), plus one reply per unresolved review point. Report branch, SHAs, build and r2r results (before/after counts on the relevant dbs), and what could not be verified.
