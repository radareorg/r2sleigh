# Fold Test Count Note

Current-state test count for `r2dec` fold/lib tests:

- Command: `cargo test -p r2dec --lib -- --list`
- Current count: `168` tests

This note is intentionally current-state only for the fold polish tranche;
no historical count archaeology is included.
